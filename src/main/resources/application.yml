spring:
  application:
    name: music-app
  datasource:
    url: jdbc:postgresql://localhost:5432/music_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true